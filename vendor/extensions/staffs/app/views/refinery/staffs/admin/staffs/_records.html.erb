<%= render 'refinery/admin/search_header', :url => refinery.staffs_admin_staffs_path %>
<div class='pagination_container'>
  <% if @staffs.any? %>
    <%= render 'staffs' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
