<% content_for :body do %>
  <ul id="animals">
    <% @animals.each do |animal| %>
      <div>
        <%= link_to (image_fu animal.photo, '104x104'), animal.photo.url %>
        <%= link_to animal.name, refinery.animals_animal_path(animal) %>
      </div>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
